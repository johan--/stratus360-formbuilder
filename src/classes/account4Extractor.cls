public class account4Extractor {@InvocableMethod(label='account4Extractor')public static List<Account> extractAccount(List<string> datas) {List<Account> result = new List<Account>();if(datas.size() > 0){if(datas[0] != ''){Map<String, Object> data = (Map<String, Object>)JSON.deserializeUntyped(datas[0]);Account s = (Account)Type.forName('Account').newInstance();for(string field : data.keySet()){if(field != 'sobjectType'){s.put(field, data.get(field));}}result.add(s);}}return result;}}